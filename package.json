{
  "name": "task-manager-backend",
  "version": "1.0.0",
  "description": "Task Manager Backend API with TypeScript and Fastify",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "clean": "rm -rf dist",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:with-server": "node scripts/test-with-server.js",
    "test:ci": "npm run build && npm run test:with-server"
  },
  "keywords": ["task-manager", "api", "typescript", "fastify", "mongodb"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "fastify": "^4.24.3",
    "@fastify/cors": "^8.4.0",
    "mongoose": "^8.0.3",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.2",
    "ts-node-dev": "^2.0.0",
    "jest": "^29.7.0",
    "axios": "^1.6.0"
  }
}
